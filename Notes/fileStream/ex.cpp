#include <fstream>    //ifstream(input) ofstream(output)

ifstream fin;

fin.open("mtFile.txt");

getLine(fin, STRING_NAME)

/*
    Cin - instances from ifstream
    Cout - instances of ofstream
*/

if(fin.is_open()){
}

fout.close();